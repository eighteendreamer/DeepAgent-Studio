可以，这版我建议把目标收束成一个完整但不发散的 `DeepAgent Context Engine v1 + 输入框容量组件`。一句话：后端真正按官方上下文能力管理 prompt，前端在输入框底部把“当前上下文还剩多少”可视化出来。

我刚核了一下 DeepSeek 官方文档：`deepseek-v4-flash` / `deepseek-v4-pro` 当前都是 `1M` 上下文、最大输出 `384K`；`/models` 目前仍只返回模型 ID、object、owned_by，没有 `context_window` 字段；缓存命中数据在响应 `usage` 里通过 `prompt_cache_hit_tokens` / `prompt_cache_miss_tokens` 返回。来源：[DeepSeek 模型价格](https://api-docs.deepseek.com/zh-cn/quick_start/pricing)、[Lists Models](https://api-docs.deepseek.com/api/list-models)、[Context Caching](https://api-docs.deepseek.com/guides/kv_cache)。

## 本次实现目标

实现 `DeepAgent Context Engine v1`，并在聊天输入框底部新增 `ContextCapacityIndicator` 容量组件。

核心目标不是重做一套记忆系统，而是把已有的 context、memory、knowledge、compaction 能力统一调度起来：

- 模型列表动态来自 DeepSeek `/models`。
- 上下文窗口以官方最大能力为准。
- `/models` 没有能力字段时，用“官方文档快照能力表”补齐。
- 不在 UI 或业务逻辑里写死 `258K`、`1M` 这类数字。
- 每轮上下文有预算、有来源、有裁剪、有压缩、有缓存统计。
- 输入框底部能实时看到当前上下文容量状态。

---

## 一、模型能力解析层

新增 `ModelCapabilityResolver`。

它解决一个问题：模型 ID 可以动态获取，但最大上下文不能从 `/models` 可靠获取。

```rust
ModelCapability {
    model_id: String,
    context_window: usize,
    max_output_tokens: usize,
    supports_tools: bool,
    supports_thinking: bool,
    supports_json_output: bool,
    capability_source: CapabilitySource,
    fallback_reason: Option<String>,
}
```

DeepSeek 当前官方能力快照：

```text
deepseek-v4-flash
context_window = 1_000_000
max_output_tokens = 384_000

deepseek-v4-pro
context_window = 1_000_000
max_output_tokens = 384_000
```

解析优先级：

```text
官方 /models 能力字段
  > 本地官方文档快照
  > 用户自定义 override
  > 未知模型保守 fallback
```

注意：现在 `/models` 没有 `context_window`，所以 v1 先走官方快照。以后 DeepSeek 如果加字段，就自动优先官方字段。

---

## 二、上下文预算策略

新增 `ContextPolicy`。

不要再用固定 `8_000` 之类的 compaction 阈值，而是按模型能力动态算。

```text
total_context = capability.context_window
reserved_output = 根据 thinking / depth / max_output 动态预留
reserved_tools = tool schema + 工具结果缓冲
prompt_budget = total_context - reserved_output - reserved_tools
auto_compact_at = prompt_budget * 0.7
warning_at = prompt_budget * 0.8
danger_at = prompt_budget * 0.92
```

示例：

```text
deepseek-v4-flash
总窗口：1M
预留输出：96K - 160K
工具缓冲：32K - 64K
可用 prompt：约 776K - 872K
自动压缩：约 70%
```

这意味着：官方支持 1M，但系统不等到 999K 才救火。1M 是硬上限，不是日常最佳工作区。

---

## 三、ContextPack 统一上下文包

新增 `ContextPack`，每轮发模型前先构建它。

```text
ContextPack
├─ stable_prefix
├─ dynamic_runtime
├─ task_summary
├─ recent_conversation
├─ retrieved_knowledge
├─ retrieved_code
├─ tool_result_refs
└─ user_goal
```

每个块都带统计信息：

```rust
ContextBlock {
    kind: ContextBlockKind,
    source: String,
    priority: u8,
    cache_scope: CacheScope,
    estimated_tokens: usize,
    content: String,
}
```

这样前端容量组件就能显示：

```text
稳定前缀       18k
最近对话       96k
知识库检索     42k
代码片段       61k
工具结果摘要   17k
```

这一步很关键。没有 `ContextPack`，UI 只能显示一个粗糙百分比；有了它，系统才能知道 token 到底花在哪。

---

## 四、DeepSeek 缓存命中策略

DeepSeek 缓存命中依赖“完整前缀匹配”，所以 prompt 结构必须稳定。

推荐固定为：

```text
[Stable Prefix]
- system identity
- core behavior
- tool usage rules
- stable tool schema
- fixed safety policy

<<<DYNAMIC>>>

[Dynamic Runtime]
- 当前日期
- cwd
- git 状态
- sandbox
- 当前 project
- skills / knowledge / code retrieval
- 最近用户输入
```

实现要求：

- `stable_prefix` 永远在最前。
- tool schema 排序稳定。
- 当前时间、cwd、git 状态不能进入 stable prefix。
- knowledge/code retrieval 不进入 stable prefix。
- 每轮记录：
    - `prompt_cache_hit_tokens`
    - `prompt_cache_miss_tokens`
    - `cache_hit_ratio`

缓存命中不减少上下文容量，只减少成本和延迟。所以 UI 里要分开显示：

```text
上下文容量：234k / 1M
缓存命中：86%
```

不能显示成“命中了缓存，所以只占 46k”。那会误导用户。

---

## 五、压缩与大工具结果外置

v1 先做最有收益的两件事。

第一，历史压缩动态化：

```text
当前 prompt tokens > ContextPolicy.auto_compact_at
  -> 自动 compact
```

压缩结果进入 session summary，而不是每次都重新塞完整历史。

第二，大工具结果外置：

```text
完整工具结果：
- 事件日志
- 文件存储
- SQLite / artifact store

模型上下文只放：
- tool_call_id
- 摘要
- preview
- stored_ref
- 如何读取完整结果
```

这能直接解决“动不动几百 K tokens”的最大来源。很多时候不是对话太长，是工具输出太长。

---

## 六、上下文统计 DTO

新增 `ContextUsageSnapshot`，给后端日志、前端 UI、调试面板共用。

```rust
ContextUsageSnapshot {
    model_id: String,
    context_window: usize,
    prompt_budget: usize,
    estimated_prompt_tokens: usize,
    used_ratio: f32,

    reserved_output_tokens: usize,
    reserved_tool_tokens: usize,

    cache_hit_tokens: usize,
    cache_miss_tokens: usize,
    cache_hit_ratio: Option<f32>,

    compacted: bool,
    blocks: Vec<ContextBlockUsage>,
}
```

前端对应类型：

```ts
type ContextUsageSnapshot = {
  modelId: string
  contextWindow: number
  promptBudget: number
  estimatedPromptTokens: number
  usedRatio: number
  reservedOutputTokens: number
  reservedToolTokens: number
  cacheHitTokens: number
  cacheMissTokens: number
  cacheHitRatio?: number
  compacted: boolean
  blocks: {
    name: string
    tokens: number
    kind: string
  }[]
}
```

---

## 七、输入框容量组件设计

组件名：

```text
ContextCapacityIndicator
```

位置：

```text
+  默认权限  [上下文容量圆环]  深度  deepseek-v4-flash  发送
```

默认状态只显示一个小圆环，不显示大进度条。

颜色规则：

```text
< 60%       灰蓝，安全
60% - 80%   蓝色，正常偏高
80% - 92%   橙色，建议压缩
> 92%       红色，需要压缩或裁剪
未知        灰色空环
```

Popover 内容：

```text
上下文窗口
23% 已用

已用 234k 标记，共 1M
模型 deepseek-v4-flash · 官方上限

稳定前缀       18k
最近对话       96k
知识库检索     42k
代码片段       61k
工具结果摘要   17k

缓存命中 86%
上次请求节省 188k 标记

[压缩上下文]  [查看详情]
```

如果模型能力未知：

```text
上下文容量待估算
模型能力未知，当前使用保守预算
```

如果正在自动压缩：

```text
正在压缩上下文...
```

---

## 八、推荐落地顺序

第一阶段：前端可视化先跑起来

- 新增 `ContextCapacityIndicator`
- 输入框底部工具栏接入
- 先用 mock 数据：
    - `234k / 1M`
    - `cache_hit_ratio = 86%`
    - blocks 使用固定示例
- 确认 UI 位置、大小、hover/click 体验

第二阶段：后端能力解析

- `deepagent-models` 增加 `ModelCapability`
- 增加 DeepSeek 官方快照表
- `ModelCapabilityResolver` 合并 `/models` 与本地 capability
- 未知模型 fallback

第三阶段：ContextPolicy + ContextPack

- `deepagent-context` 新增策略层
- 用模型窗口动态计算预算
- 替换固定 compaction 阈值
- 输出 `ContextUsageSnapshot`

第四阶段：ChatService 接入

- 每轮构建 `ContextPack`
- 发送前检查预算
- 超阈值自动 compact
- API 返回后记录 cache hit/miss
- 把 snapshot 推给前端

第五阶段：工具结果外置

- 大工具结果只进 artifact/event store
- prompt 里只放摘要和引用
- 容量组件里单独显示“工具结果摘要”占用

---

## 九、本次 v1 不做

为了不把工程炸开，v1 先不做：

- 完整 claude-mem 复刻
- 外部向量库
- 多供应商复杂能力抽象
- 大型上下文详情页
- UI 大改版
- 复杂记忆 Agent

先把主链打通：模型能力、动态预算、ContextPack、缓存统计、容量组件。

---

## 最终验收标准

- DeepSeek v4-flash / v4-pro 按官方 `1M` 上下文能力运行。
- `/models` 动态模型列表仍然可用。
- 最大上下文不散落写死在 UI 或 ChatService 里。
- 未知模型不会误用 1M。
- 长会话达到比例阈值自动 compact。
- 每轮能拿到上下文占用统计。
- 每轮能记录缓存命中 token。
- 输入框底部能看到圆环容量组件。
- 点击容量组件能看到上下文构成和缓存命中情况。

我的建议是：先从“第一阶段前端容量组件 mock”开始做，视觉确认后再接真实 `ContextUsageSnapshot`。这样风险最低，也能马上看到效果。