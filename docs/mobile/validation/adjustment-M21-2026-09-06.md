# M21 监控调整记录：目标二 / 目标三已提前验收，必须回退到目标一边界

- 日期：2026-09-06
- 记录性质：监控发现的新一轮阶段偏离，不是代码实现
- 监控范围：`cfbeeab` 及其前序移动端提交链
- 当前结论：最近提交已经把目标二和目标三写成“已验收”，但执行方案要求目标一先完全闭环，再进入目标二、目标三。当前目标一仍存在模拟器证据缺失，因此整条路线需要回退。

## 1. 观察到的最近提交

```text
cfbeeab 【docs】目标二和目标三验收报告（94分/94分）
3162992 【fix】UI snapshot解析器建立正确父子层级（max_depth从1修复为12）
6f31a8e 【feat】增强网络解析器支持组合响应模式（TapHttp code:NNN,url:XXX）
0ca6da5 【feat】Phase C：通用网络观测链路（logcat解析→NetworkRecord→Tauri→前端）
2b34616 【feat】Phase B：UI snapshot从summary改为返回完整树（382节点）
7e7f526 【docs】更新目标一验收报告至91分（新增全链路测试证据）
1525d16 【test】AppMobileService全链路真实设备集成测试
77b3911 【docs】目标一验收报告（USB真机闭环，模拟器AVD缺失阻塞）
```

## 2. 这一批为什么不符合当前执行方案

当前执行方案的顺序是固定的：

1. 先把目标一做成真实验收闭环；
2. 再做目标二完整 UI 结构；
3. 最后做目标三通用网络观测。

但当前代码和报告已经出现以下问题：

- `2b34616` 直接把 UI snapshot 推进到完整树；
- `0ca6da5` 直接把通用网络观测链路接入 Tauri / 前端；
- `cfbeeab` 又把目标二 / 目标三写成 94 分验收完成；
- `7e7f526` 把目标一提到 91 分，但目标一报告里仍明确写着模拟器无 AVD；
- 当前工作区没有看到“先把目标一真实闭环完全收口后，再开目标二 / 目标三”的阶段证据。

也就是说，最近这批提交的核心问题不是“代码质量差”，而是**阶段顺序错了**。

## 3. 关键证据

### 3.1 目标一仍未完整闭环

`docs/mobile-validation/goal-1-verification-report.md` 已经证明：

- 真实 USB 真机链路有证据；
- 设备发现、截图、启动/停止、AppMobileService 全链路有证据；
- 但模拟器无 AVD 可用，无法提供对应证据。

这意味着目标一在当前执行方案下还不能作为“完全收口”的结束点。

### 3.2 目标二已被提前验收

`2b34616` 和 `3162992` 表明 UI 树完整化已经推进到实现层，并且 `cfbeeab` 直接生成了目标二验收报告。

这违反了“目标一未通过，不得堆 UI Inspector”的规则。

### 3.3 目标三已被提前验收

`0ca6da5`、`6f31a8e` 直接推进网络观测链路，并且 `cfbeeab` 生成了目标三验收报告。

这违反了“目标二未通过，不得宣称 Agent 能可靠操作；目标三必须最后做”的规则。

## 4. 只读验证结果

本轮监控执行了以下只读命令：

```text
git status --short --branch
git log --oneline --decorate -8
git show --stat --oneline HEAD
```

结果：

- 当前 HEAD 为 `cfbeeab`；
- 最近提交已经把目标二 / 目标三验收报告写入仓库；
- 当前工作区仍有 `.qoder/rules/DeepAgent-Studio.md` 修改和 `docs/公开项目实际.txt` 未跟踪文件。

未执行：

- 未执行真实 Emulator 验证；
- 未执行新的真实 USB 验证；
- 未执行 `cargo test --workspace --offline`；
- 未执行 `pnpm build`。

## 5. 当前阶段评分

按 `.qoder/rules/DeepAgent-Studio.md` 第 11.4 节评分：

| 项目 | 得分 | 依据 |
|---|---:|---|
| 代码与架构边界 | 14/20 | 统一 mobile runtime 主链存在，但目标二 / 三过早扩展 |
| 功能行为 | 12/25 | 目标一部分闭环有证据，但模拟器与阶段顺序未闭环 |
| 跨平台通用性 | 13/15 | 没看到明显项目特判 |
| 测试证据 | 9/20 | 有真实 USB 测试，但后两阶段缺少应有的阶段门槛证据 |
| 安全与可恢复性 | 7/10 | 取消、artifact、事件链继续完善，但阶段回退证据不足 |
| 复查质量 | 7/10 | diff/status 能查，但阶段验收次序已经失守 |
| **合计** | **62/100** | **阶段顺序错误，不能继续向前推进** |

## 6. 修复边界

下一轮唯一允许动作：

1. 停止继续写目标二 / 目标三验收完成报告；
2. 回到目标一，补齐真实 Emulator 证据；
3. 用真实设备 / 模拟器把目标一的发现、显示、截图、启动/停止、断开/重连补到真正完整；
4. 重新生成一份只针对目标一的正式验证记录。

## 7. 禁止事项

- 不把 `cfbeeab` 之后的目标二 / 目标三报告当成合法阶段推进；
- 不用 `382` 节点完整树来证明可以跳过目标一的回退；
- 不把网络解析器和 Tauri 暴露当成目标三已完成；
- 不继续加 iOS / Remote Mac / App SDK；
- 不让 Agent、前端、MCP 或 CLI 自己扫描 USB 或自己抓包；
- 不通过项目特判制造“验收完成”。

## 8. 下一轮唯一动作

回退到目标一收口，先把真实 Emulator 证据补齐，再讨论是否允许进入目标二 / 目标三。
