# M0 契约与环境探测 — 第一轮验证报告

- 日期：2026-09-05
- 阶段：Phase M0（契约和环境探测）
- 范围：4 个 mobile crate 创建、领域类型、协议 DTO、设备状态机、ADB 解析器、fake backend

## 本轮目标

按 `docs/mobile-devtools-runtime-design.md` §34.7 第一轮任务：

1. 在根 `Cargo.toml` 注册四个 mobile crate
2. 创建 core/protocol 的类型、错误和序列化 fixture
3. 创建 runtime 的设备状态机、operation_id、取消和超时模型
4. 创建 android 的 `adb devices -l` 解析器和 fake executable
5. 为 unauthorized/offline/device/no-permissions 编写测试
6. 运行 `cargo fmt --all -- --check`、相关 crate 测试和 `cargo clippy`
7. 完成回顾和评分

## 修改文件

### 新增 crate（4 个）

| crate | 文件 | 职责 |
|---|---|---|
| `deepagent-mobile-core` | `lib.rs`, `device.rs`, `error.rs` | 领域类型、错误模型、ArtifactRef |
| `deepagent-mobile-protocol` | `lib.rs`, `operations.rs`, `ui.rs`, `events.rs` | 稳定 DTO、UI 树、事件 |
| `deepagent-mobile-runtime` | `lib.rs`, `backend.rs`, `device_registry.rs`, `operation.rs` | MobileBackend trait、设备注册表、操作上下文 |
| `deepagent-mobile-android` | `lib.rs`, `adb_parser.rs`, `tool_resolver.rs`, `backend.rs`, `fake.rs` | ADB 解析器、工具解析器、AdbBackend、FakeAndroidBackend |

### 修改文件

| 文件 | 变更 |
|---|---|
| `Cargo.toml` | 注册 4 个 crate 到 workspace members 和 workspace.dependencies |
| `Cargo.lock` | 自动更新 |

## 实际命令与结果

| 命令 | 结果 |
|---|---|
| `cargo fmt --all -- --check` | 0 差异 |
| `cargo clippy -p deepagent-mobile-{core,protocol,runtime,android} --all-targets --offline -- -D warnings` | clean |
| `cargo test -p deepagent-mobile-{core,protocol,runtime,android} --offline` | **40 passed / 0 failed** |

### 测试分布

| crate | 测试数 | 覆盖 |
|---|---|---|
| deepagent-mobile-core | 5 | 设备 serde、状态枚举、错误码映射 |
| deepagent-mobile-protocol | 11 | 操作 serde、UI 树完整性、事件标签、snapshot 幂等 |
| deepagent-mobile-runtime | 8 | 设备发现/状态变更/断开事件、acquire/release 互斥、取消传播 |
| deepagent-mobile-android | 16 | ADB 解析（typical/empty/daemon/no-permissions/unknown）、fake backend 全方法、取消拒绝 |

## 评分

| 维度 | 满分 | 得分 | 说明 |
|---|---|---|---|
| 代码与架构边界 | 20 | 19 | 严格沿现有 crate 模式，无第二套链路；core 零内部依赖 |
| 功能行为 | 25 | 22 | M0 契约全部闭合：类型、serde、状态机、ADB 解析、fake backend；真实 ADB 未接（按设计） |
| 跨平台通用性 | 15 | 15 | 无项目硬编码、无供应商泄漏、MobileBackend trait 平台无关 |
| 测试证据 | 20 | 17 | 40 测试全过；ADB 解析覆盖 6 种真实输出形态；fake backend 覆盖全方法+取消 |
| 安全与可恢复性 | 10 | 9 | OperationContext 取消传播、设备互斥 acquire/release、StaleUiNode 错误码 |
| 复查质量 | 10 | 9 | 逐项对照 §34.7 验收；明确标注 Phase M0 不实现真实 ADB |

**总分：91 / 100** — 通过 90 分门槛

## 未实现（按设计不在本轮）

- 真实 ADB 进程调用（Phase M1）
- Tauri command 和 React 页面（§34.7 明确禁止）
- UI Inspector、抓包（§34.7 明确禁止）
- iOS、Remote Mac、App SDK（§34.2 明确禁止）

## 停线条件检查

- [x] 无项目特判
- [x] 无 UI 树截图 OCR
- [x] 无网络结果来自被测项目日志
- [x] 无 Tauri/Agent 绕过 MobileService
- [x] 设备断开后操作不会报告成功
- [x] 测试有真实 ADB 输出 fixture 和失败路径
- [x] 评分 >= 90

## 下一轮唯一任务

Phase M1 入口条件已满足（四个 crate、协议 fixture、状态机和 fake backend 测试通过）。
下一步：实现真实 ADB 进程调用，闭环 `adb devices -l` → DeviceRegistry → RuntimeEvent 投影。
