---
description: Restore and inspect a WeChat mini-program package with wedecode
allowed-tools: Bash(node:*), Bash(npm:*)
---
Use the built-in wedecode runtime to inspect the requested wxapkg input. Write restored files under a dedicated output directory in the current project and preserve the original package.

Request:
$ARGUMENTS
