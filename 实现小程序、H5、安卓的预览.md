我查了一圈后，结论是：**可以做系统内置模拟器，但不要照抄微信开发者工具；应该做 DeepAgent 自己的 MiniApp Runtime。**

依据大概是这些：

- 小程序本质是“逻辑层 + 视图层”分离，逻辑层通过 `setData` 驱动视图更新，视图层不能被业务 JS 直接操作 DOM。[参考](https://www.cnblogs.com/goloving/p/14663950.html)
- WXML 依靠数据绑定、条件渲染、列表渲染来更新 UI。[参考](https://www.tencentcloud.com/zh/document/product/1219/60345)
- Taro 的做法是模拟 DOM/BOM、事件系统、bridge，再把动态 DOM 树序列化给小程序模板渲染。[参考](https://docs.taro.zone/en/docs/implement-note)
- uni-app 的 runtime 也明确拆成基础框架、组件、API 三部分。[参考](https://uniapp.dcloud.net.cn/tutorial/)
- 小程序转 H5 可行路线是把 `wxml/wxss/js/json` 合并转换成 Vue/HTML/CSS/JS，再补运行时能力。[参考](https://ask.dcloud.net.cn/article/37086)

所以方案应该是：**内置一个“小程序运行沙箱 + 编译器 + Inspector”，而不是启动外部开发工具。**

**总体架构**
```text
DeepAgent Desktop
  └─ MiniAppSimulatorDialog 弹窗
      ├─ DeviceFrame 机型外壳
      ├─ RuntimeWebView 预览运行区
      ├─ InspectorPanel 元素选择/样式/源码定位
      ├─ ConsolePanel console / wx API 日志
      ├─ NetworkPanel wx.request mock
      └─ Toolbar 编译、刷新、机型、横屏、截图

deepagent-app-core
  └─ miniapp_service.rs
      ├─ detect_project()
      ├─ compile_project()
      ├─ start_runtime_server()
      ├─ watch_and_recompile()
      ├─ simulator_status()
      └─ runtime_event_stream()

apps/desktop/src/miniapp-runtime
  ├─ compiler/
  ├─ renderer/
  ├─ bridge/
  ├─ wx-api/
  ├─ inspector/
  └─ device/
```

**运行模式**
做两条路线。

**模式 A：框架项目快速预览**
适合 Taro / uni-app / Remax / mpvue 这类项目。

实现方式：

```text
识别项目类型
  → 调用项目自己的 H5/dev 构建命令
  → 启动本地 dev server
  → 弹窗 WebView 加载 localhost
  → 注入 Inspector SDK
```

优点：最快可用，兼容度高。  
缺点：依赖项目本身能编译到 H5。

**模式 B：原生微信小程序内置 runtime**
适合标准微信小程序目录：

```text
app.json
app.js
app.wxss
pages/foo/foo.json
pages/foo/foo.wxml
pages/foo/foo.wxss
pages/foo/foo.js
```

实现方式：

```text
project.config.json/app.json
  → 页面路由表
WXML
  → AST
  → Render Function
WXSS
  → CSS transform
JS
  → sandbox runtime
wx.*
  → mock/bridge API
RuntimeWebView
  → 渲染页面
Inspector
  → 元素选择和源码定位
```

**编译器设计**
建议放在 `apps/desktop/src/miniapp-runtime/compiler`，用 TypeScript 做，Rust 只负责任务调度、文件监听和进程管理。

编译阶段：

```text
1. ProjectDetector
   - 检测 project.config.json
   - 检测 app.json
   - 检测 pages 字段
   - 检测 Taro/uni-app package.json

2. ManifestCompiler
   - 解析 app.json
   - 解析 page.json
   - 生成 route manifest
   - 生成 tabBar 配置
   - 生成 usingComponents 依赖图

3. WxmlCompiler
   - wxml -> AST
   - 支持 {{ expr }}
   - 支持 wx:if / wx:elif / wx:else
   - 支持 wx:for / wx:key
   - 支持 bindtap / catchtap / capture-bind
   - 支持 template / include / import
   - 输出 render(ctx): VNode[]

4. WxssCompiler
   - wxss -> css
   - rpx -> px，按 device width 计算
   - @import 展开
   - app.wxss + page.wxss + component.wxss 作用域合并
   - 后续支持 less/sass

5. JsCompiler
   - esbuild/babel 处理 JS/TS
   - 包装 Page / Component / App 构造器
   - 禁用危险 Node API
   - 输出 sandbox bundle

6. SourceMapGenerator
   - 每个 WXML 节点保留 file/line/column
   - 每个 WXSS rule 保留来源
   - Inspector 点击元素能跳源码
```

**运行时设计**
运行时建议模拟小程序双线程，但在桌面里用浏览器能力实现。

```text
Logic Sandbox
  - Web Worker 或 iframe sandbox
  - 执行 app.js/page.js/component.js
  - 提供 App/Page/Component/getApp/getCurrentPages/wx

View Runtime
  - React 或轻量自研 renderer
  - 根据 VNode 渲染真实 DOM
  - 每个节点带 data-mini-node-id

Bridge
  - logic -> view: setData patch
  - view -> logic: event dispatch
  - runtime -> host: console/network/inspector events
```

推荐内部实现：

```text
PageInstance {
  route
  data
  setData(patch)
  lifecycle: onLoad/onShow/onReady/onHide/onUnload
  methods
}

ComponentInstance {
  properties
  data
  observers
  lifetimes
  pageLifetimes
  methods
}

RuntimeBridge {
  emitViewPatch(pageId, patch)
  emitEvent(pageId, nodeId, eventName, detail)
  emitConsole(level, args)
  emitNetwork(req/res)
}
```

**基础组件优先级**
第一阶段只做高频组件：

```text
view
text
image
button
input
textarea
scroll-view
swiper
swiper-item
navigator
form
checkbox
radio
switch
picker
icon
```

每个组件映射为 DOM + 样式 + 事件：

```text
<view> -> div
<text> -> span
<image> -> img
<button> -> button
<scroll-view> -> div overflow
<navigator> -> runtime.navigateTo()
```

**wx API Mock**
第一批必须做：

```text
wx.navigateTo
wx.redirectTo
wx.reLaunch
wx.switchTab
wx.navigateBack
wx.request
wx.showToast
wx.showModal
wx.showLoading
wx.hideLoading
wx.getStorage
wx.setStorage
wx.removeStorage
wx.getSystemInfo
wx.login
wx.getUserProfile
wx.chooseImage
```

API 面板支持配置返回值：

```text
wx.login -> mock code
wx.getUserProfile -> mock userInfo
wx.request -> mock response table
wx.getLocation -> mock lat/lng
```

**元素选择 Inspector**
这个是 DeepAgent 可以做得比官方更贴近代码的地方。

渲染时给每个节点生成：

```html
<div data-mini-node-id="n_123"></div>
```

节点元信息：

```ts
MiniNodeMeta {
  id
  tag
  file
  line
  column
  componentPath
  bindings
  events
  wxssRules
}
```

Inspector 功能：

```text
- hover 高亮
- click 选中
- 组件树
- WXML 源码位置
- WXSS 规则来源
- computed style
- data/properties 当前值
- bindtap/catchtap 事件列表
- 一键打开对应文件/行
```

**机型切换**
前端纯配置即可：

```ts
DevicePreset {
  id: "iphone-15-pro"
  name: "iPhone 15 Pro"
  width: 393
  height: 852
  dpr: 3
  statusBar: 54
  safeAreaTop: 59
  safeAreaBottom: 34
  platform: "ios"
}
```

支持：

```text
- iPhone SE
- iPhone 12/13/14/15
- iPhone Pro Max
- Pixel 5/7/8
- Android compact
- iPad mini
- 自定义宽高
- 横屏
- 缩放比例
- dark mode
- 网络状态 mock
```

`rpx` 换算：

```ts
px = rpx * device.width / 750
```

**弹窗集成方式**
你要求“不影响现有架构”，所以建议不改主布局，只加一个入口和一个独立弹窗。

```text
apps/desktop/src/components/miniapp/
  MiniAppSimulatorDialog.tsx
  DeviceFrame.tsx
  SimulatorToolbar.tsx
  InspectorPanel.tsx
  ConsolePanel.tsx
  NetworkMockPanel.tsx
```

入口可以放：

```text
Sidebar 项目菜单
ProjectMap Panel 按钮
Command Palette: Open Mini Program Simulator
```

Tauri command：

```rust
miniapp_detect_project(project_path)
miniapp_compile(project_path, page_route)
miniapp_start_runtime(project_path)
miniapp_stop_runtime(project_path)
miniapp_read_runtime_events(session_id)
miniapp_open_source(file, line, column)
```

**Android 模拟器内置方案**
Android 这部分不要做成完整 Android 开发 IDE，也不要自研 Android 模拟器内核。推荐定位是：

```text
DeepAgent 内置 Android Emulator Launcher + Device Viewer
```

也就是用户不需要打开 Android Studio，直接在 DeepAgent 里启动、查看和切换 Android 模拟器；底层复用 Google 官方 Android Emulator、ADB、SDK Command Line Tools。

核心边界：

```text
要做：
- Android SDK 托管安装
- AVD 创建/启动/停止
- 模拟器画面内置展示
- 机型切换
- 横竖屏
- 截图
- 可选 APK 安装/打开 URL

暂不做：
- Gradle 深度集成
- Java/Kotlin 调试
- Android Profiler
- Layout Inspector
- 完整 Android Studio 替代
```

**Android SDK 托管安装**
可以把 Android SDK 安装进 DeepAgent 管理的目录，但不建议直接打包进安装包。原因是 SDK、emulator、system image 体积很大，且不同系统和 CPU 架构需要不同镜像。

推荐目录：

```text
Windows:
%APPDATA%/DeepAgent/runtimes/android-sdk

macOS:
~/Library/Application Support/DeepAgent/runtimes/android-sdk

Linux:
~/.local/share/deepagent/runtimes/android-sdk
```

目录结构：

```text
runtimes/android-sdk/
  cmdline-tools/
  platform-tools/
  emulator/
  platforms/
  system-images/
  avd/
```

首次打开 Android 模拟器时走安装向导：

```text
打开 Android 模拟器
  → 检测系统已有 ANDROID_HOME / ANDROID_SDK_ROOT
  → 检测 DeepAgent 托管 SDK
  → 不存在则下载 Android Command Line Tools
  → 用户确认 Android SDK license
  → 安装 platform-tools / emulator / system image
  → 创建默认 AVD
  → 启动模拟器
```

底层命令示例：

```bash
sdkmanager --sdk_root=<deepagent_android_sdk> "platform-tools" "emulator"
sdkmanager --sdk_root=<deepagent_android_sdk> "platforms;android-35"
sdkmanager --sdk_root=<deepagent_android_sdk> "system-images;android-35;google_apis;x86_64"
avdmanager create avd -n DeepAgent_Pixel_7 -k "system-images;android-35;google_apis;x86_64"
emulator -avd DeepAgent_Pixel_7
```

Windows 上优先安装 `x86_64` 镜像；Apple Silicon 优先安装 `arm64-v8a` 镜像。DeepAgent 只负责选择合适包和维护下载状态。

**Android 模拟器显示方式**
分两阶段做。

第一阶段先管理官方 emulator 进程：

```text
DeepAgent 弹窗
  → 选择设备
  → 启动 emulator.exe
  → 展示启动状态/日志
  → 提供停止、重启、截图、旋转按钮
```

这个阶段最快可落地，体验上已经是不打开 Android Studio，由 DeepAgent 统一管理。

第二阶段实现真正内置画面：

```text
Android Emulator
  → ADB 连接 emulator-5554
  → scrcpy/ADB 视频流
  → DeepAgent AndroidDeviceFrame 渲染画面
  → 鼠标/键盘事件转发给设备
```

推荐优先用 scrcpy/ADB 镜像路线，而不是强行把 emulator 原生窗口句柄嵌进 Tauri。窗口嵌入在 Windows 上可做，但跨平台、缩放、焦点、输入法、窗口生命周期会更复杂。

**Android 设备切换**
Android 不建议像 H5 那样只改 CSS 宽高。更稳定的做法是预置多个 AVD：

```text
DeepAgent_Pixel_5
DeepAgent_Pixel_7
DeepAgent_Pixel_8
DeepAgent_Tablet
DeepAgent_Fold
DeepAgent_Custom
```

切换流程：

```text
停止当前 AVD
  → 启动目标 AVD
  → 重新绑定画面流
  → 刷新设备状态
```

第一版可以只提供 3 个默认设备：

```text
Pixel 7：常规手机
Pixel 8 Pro：大屏手机
Pixel Tablet：平板
```

**Android 模块拆分**
前端建议放在统一模拟器中心下，不和小程序 runtime 混在一起：

```text
apps/desktop/src/components/simulator/
  SimulatorCenterDialog.tsx
  SimulatorTabs.tsx
  AndroidEmulatorPanel.tsx
  AndroidDeviceSelector.tsx
  AndroidDeviceFrame.tsx
  AndroidToolbar.tsx
  AndroidInstallGuide.tsx
```

Rust/Tauri 后端：

```text
apps/desktop/src-tauri/src/android_sdk.rs
apps/desktop/src-tauri/src/android_emulator.rs
apps/desktop/src-tauri/src/adb.rs
```

核心 command：

```rust
android_check_environment()
android_install_sdk_packages(packages)
android_accept_licenses()
android_list_avds()
android_create_avd(profile)
android_start_avd(avd_name)
android_stop_avd(avd_name)
android_list_devices()
android_take_screenshot(device_id)
android_rotate_device(device_id)
android_install_apk(device_id, apk_path)
android_open_url(device_id, url)
```

数据结构：

```ts
AndroidSdkStatus {
  sdkRoot: string
  hasCmdlineTools: boolean
  hasPlatformTools: boolean
  hasEmulator: boolean
  installedSystemImages: string[]
  missingPackages: string[]
}

AndroidAvdProfile {
  id: string
  name: string
  device: "pixel_7" | "pixel_8_pro" | "pixel_tablet" | "custom"
  apiLevel: number
  image: string
  width?: number
  height?: number
  density?: number
}

AndroidEmulatorSession {
  sessionId: string
  avdName: string
  deviceId?: string
  status: "installing" | "starting" | "booting" | "running" | "stopped" | "error"
  screenMode: "external-window" | "embedded-stream"
}
```

**Android MVP 定义**
第一版 Android 只承诺模拟器能力：

```text
- DeepAgent 自动检测/安装 Android SDK
- DeepAgent 自动创建默认 AVD
- DeepAgent 内启动/停止 Android Emulator
- 支持 3 种设备预设
- 支持横竖屏/重启/截图
- 支持打开 URL
- 可选支持安装 APK
- 第二阶段支持内置画面镜像
```

这样用户体验接近 Android Studio 的内置 Running Devices，但 DeepAgent 不承担完整 Android IDE 的复杂度。

**开发阶段**
**Phase 1：内置弹窗 + 框架项目预览**
目标：1-2 周可见效果。

- 弹窗 UI
- 机型切换
- 加载 localhost dev server
- 支持 Taro/uni-app H5 预览
- 控制台日志
- 刷新/重启编译
- 基础截图

**Phase 2：原生小程序 MVP Runtime**
目标：能跑简单微信原生 demo。

- 解析 `app.json`
- 页面路由
- WXML 基础语法
- WXSS/rpx
- `Page({ data, onLoad, methods })`
- `setData`
- `bindtap`
- 基础组件：view/text/image/button/input/scroll-view
- wx.request/storage/toast/modal mock

**Phase 3：Inspector**
目标：可元素选择、定位源码。

- hover overlay
- click select
- 组件树
- 样式面板
- data/properties 面板
- event handlers 面板
- 源码定位

**Phase 4：自定义组件**
目标：跑中等复杂项目。

- `Component({ properties, data, methods })`
- `usingComponents`
- properties 传递
- slots
- observers
- lifetimes
- behaviors 简化版

**Phase 5：高级小程序能力**
目标：更接近真实开发工具。

- tabBar
- 分包
- npm miniprogram modules
- WXS
- 页面栈
- network mock 录制/回放
- 权限 mock
- 地理位置 mock
- 图片选择 mock
- 编译诊断面板

**关键技术选择**
推荐：

```text
编译 JS/TS：esbuild
解析 JS：@babel/parser
转换 JS：@babel/traverse / @babel/generator
解析 WXSS：postcss
解析 WXML：自研 parser 或 htmlparser2/htmlparser2-like XML parser
文件监听：已有 notify
运行隔离：iframe sandbox 或 Web Worker
预览承载：Tauri Webview / iframe
UI：现有 React
```

**不要一开始做的事**
这些会拖垮周期：

```text
- 完整复刻微信 DevTools
- 完整兼容所有 wx API
- 直接嵌入官方模拟器窗口
- 一开始支持所有平台小程序
- 一开始支持 Skyline 渲染
- 一开始支持完整 WXS
```

**最落地的 MVP 定义**
第一个版本建议只承诺：

```text
- 原生微信小程序简单页面可预览
- Taro/uni-app 可通过 H5 dev server 预览
- 支持 8-10 种机型切换
- 支持 setData 更新
- 支持 bindtap
- 支持 view/text/image/button/input/scroll-view
- 支持 wx.request/storage/toast mock
- 支持元素选择和源码定位
- Android 侧支持 SDK 托管安装、默认 AVD 创建、模拟器启动/停止、3 种设备预设、截图/旋转/打开 URL
```

**最终形态**
DeepAgent 内置预览系统应该定位成：

```text
小程序/H5：日常开发预览 + AI 调试 + 元素选择 + 源码定位 + API Mock
Android：内置 SDK 管理 + Emulator 启动/显示 + 设备切换 + 轻量设备操作
```

而不是：

```text
微信官方开发者工具的完整替代品
Android Studio 的完整替代品
```

这样做可落地，且和 DeepAgent 的优势一致：你不只是“看页面”，还能让 AI 直接基于元素、源码、运行日志、网络 mock 去帮你改。
